const SubCounter = ({ count3 }) => { // Destructing props accessing count3
  return (
    <div>Count3 is {count3}</div> // Displaying the value of 'count3'
  );
};

export default SubCounter; // Exporting the SubCounter component as default
